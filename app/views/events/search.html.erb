<% provide(:title, 'Search results') %>
<h1> Search Results </h1>

<%= will_paginate %>

<ul class="events">
  <% @events.each do |event| %>
	<%= render event, event: event %>
  <% end %>
  <% if @events.count == 0 %>
	No events matched your search term
  <% end %>
</ul>

<%= will_paginate %>
